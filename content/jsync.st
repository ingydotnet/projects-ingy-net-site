JSYNC is my simple proposal to allow anything that can be serialized in YAML, to be encoded as JSON. The goal is to get stable inter-language serialization quickly, by leveraging the many, many JSON implementations.

*See*: http://jsync.org
